import 'package:flutter/material.dart';

class LiveWatchPage extends StatelessWidget {
  const LiveWatchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('直播观看')),
      body: const Center(child: Text('直播观看页（占位）')),
    );
  }
}
