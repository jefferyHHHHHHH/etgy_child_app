openapi: 3.0.3
info:
  title: ETGY Child API
  version: 0.1.0
servers:
  - url: https://dev-api.example.com
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
